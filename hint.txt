create a table 

docker exec -it ecomm-mysql mysql -uroot -ppassword -e "CREATE DATABASE ecomm;"

drop a table

docker exec -it ecomm-mysql mysql -uroot -ppassword -e "DROP DATABASE ecomm;"

create a migration

docker run -it --rm \
  --network host \
  -v "$(pwd)/db:/db" \
  migrate/migrate \
  create -ext sql -dir /db/migrations init_schema
  
start migration !! 

docker run -it --rm \
  --network host \
  -v "$(pwd)/db:/db" \
  migrate/migrate \
  -path=/db/migrations \
  -database "mysql://root:password@tcp(localhost:3306)/ecomm" up
  
  3th table 
docker run -it --rm \
  --network host \
  -v "$(pwd)/db:/db" \
  migrate/migrate:v4.17.0 \
  create -ext sql -dir /db/migrations add_sessions_table